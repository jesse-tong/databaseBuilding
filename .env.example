# Example environment variables for the CV management system
# Note: Replace the values with your actual credentials and paths.
# When running the application, change the file name to .env
OPENAI_API_KEY=openai_secret_key_here
DEFAULT_MODEL=gpt-4.1-mini
DEFAULT_CV_STORAGE_PATH=./cv_storage
DEFAULT_VECTORDB_STORAGE_PATH=./vectordb
MARIADB_USERNAME=db_user
MARIADB_PASSWORD=db_password
MARIADB_HOST=localhost
MARIADB_PORT=3306
MARIADB_DATABASE=db_name